[project]
name = "pluto-gateway-ui"
version = "0.1.0"
description = "PlutoTV to TVHeadend gateway UI"
requires-python = ">=3.10"
dependencies = [
  "fastapi>=0.110",
  "uvicorn[standard]>=0.27",
  "httpx>=0.27",
  "pydantic>=2.6",
]

[tool.uvicorn]
factory = false
host = "0.0.0.0"
port = 8788
